#  Patches

Since device uses Iris5 with was never dupported by anything, and SoftIris with is still NOT officially merged with sm8450 plaform, we have to apply some patches.

### Just run `apply.sh` from this directory

Also will apply patch from common directory as well